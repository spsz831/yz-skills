Scene guidance: if the user request already defines the setting, follow the user's setting directly. Only when the request is underspecified may you borrow a fallback environmental idea such as {scene}. The environment should stay simple and quiet, with low information density but enough structure to avoid emptiness. Background rule: {background_rule}. Preserve elegant emptiness, clean horizon spacing, clear environmental layering, and controlled depth across foreground, midground, and distance. Include only a few restrained environmental support elements, never dense clutter and never an overfilled scene. The environment must contain no text-bearing surfaces. Background elements must remain architectural or environmental only, never human-shaped.
